---
# ansible-playbook -u lookup project/postgres_norvm.yml --extra-vars 'target=lookup'
- hosts: '{{target}}'
  vars:
    app_path: '/data/{{ansible_user_id}}/app'
    db_password: "{{ lookup('password', playbook_dir + '/../credentials/' + inventory_hostname + '/' + ansible_user_id + '_mongodb length=15') }}"
  vars_files:
    - "{{playbook_dir}}/../../db.yml"
  roles:
    - { role: postgres }
    - { role: env }
